
    function hideShow(e){

	(e.preventDefault) ? e.preventDefault() : (e.returnValue = false);
	
	var targetID = (e.target || e.srcElement).getAttribute("data-toShow"),
		elementToShow = document.getElementById(targetID);

	elementToShow.style.height = (elementToShow.offsetHeight == 0 ? elementToShow.scrollHeight : "0") + 'px';

}

var a = document.querySelectorAll("[data-toShow]"),
	i = 0,
	l = a.length;

for (; i<l; i++) document.addEventListener ? a[i].addEventListener("click", hideShow, false) : a[i].attachEvent("onclick", hideShow);
           
       function hideShow(e){

	(e.preventDefault) ? e.preventDefault() : (e.returnValue = false);
	
	var targetID = (e.target || e.srcElement).getAttribute("data-toShow"),
		elementToShow = document.getElementById(targetID);

	elementToShow.style.height = (elementToShow.offsetHeight == 0 ? elementToShow.scrollHeight : "0") + 'px';

}

var b = document.querySelectorAll("[data-toShow]"),
	i = 0,
	l = a.length;

for (; i<l; i++) document.addEventListener ? a[i].addEventListener("click", hideShow, false) : a[i].attachEvent("onclick", hideShow);
            
          
    
  
    function hideShow(e){

	(e.preventDefault) ? e.preventDefault() : (e.returnValue = false);
	
	var targetID = (e.target || e.srcElement).getAttribute("data-toShow"),
		elementToShow = document.getElementById(targetID);

	elementToShow.style.height = (elementToShow.offsetHeight == 0 ? elementToShow.scrollHeight : "0") + 'px';

}

var c = document.querySelectorAll("[data-toShow]"),
	i = 0,
	l = a.length;

for (; i<l; i++) document.addEventListener ? a[i].addEventListener("click", hideShow, false) : a[i].attachEvent("onclick", hideShow);
           
       function hideShow(e){

	(e.preventDefault) ? e.preventDefault() : (e.returnValue = false);
	
	var targetID = (e.target || e.srcElement).getAttribute("data-toShow"),
		elementToShow = document.getElementById(targetID);

	elementToShow.style.height = (elementToShow.offsetHeight == 0 ? elementToShow.scrollHeight : "0") + 'px';

}

var d = document.querySelectorAll("[data-toShow]"),
	i = 0,
	l = a.length;

for (; i<l; i++) document.addEventListener ? a[i].addEventListener("click", hideShow, false) : a[i].attachEvent("onclick", hideShow);
            
          
    
    function hideShow(e){

	(e.preventDefault) ? e.preventDefault() : (e.returnValue = false);
	
	var targetID = (e.target || e.srcElement).getAttribute("data-toShow"),
		elementToShow = document.getElementById(targetID);

	elementToShow.style.height = (elementToShow.offsetHeight == 0 ? elementToShow.scrollHeight : "0") + 'px';

}

var e = document.querySelectorAll("[data-toShow]"),
	i = 0,
	l = a.length;

for (; i<l; i++) document.addEventListener ? a[i].addEventListener("click", hideShow, false) : a[i].attachEvent("onclick", hideShow);
           
       function hideShow(e){

	(e.preventDefault) ? e.preventDefault() : (e.returnValue = false);
	
	var targetID = (e.target || e.srcElement).getAttribute("data-toShow"),
		elementToShow = document.getElementById(targetID);

	elementToShow.style.height = (elementToShow.offsetHeight == 0 ? elementToShow.scrollHeight : "0") + 'px';

}

var f = document.querySelectorAll("[data-toShow]"),
	i = 0,
	l = a.length;

for (; i<l; i++) document.addEventListener ? a[i].addEventListener("click", hideShow, false) : a[i].attachEvent("onclick", hideShow);
            
          
    
    
    function hideShow(e){

	(e.preventDefault) ? e.preventDefault() : (e.returnValue = false);
	
	var targetID = (e.target || e.srcElement).getAttribute("data-toShow"),
		elementToShow = document.getElementById(targetID);

	elementToShow.style.height = (elementToShow.offsetHeight == 0 ? elementToShow.scrollHeight : "0") + 'px';

}

var g = document.querySelectorAll("[data-toShow]"),
	i = 0,
	l = a.length;

for (; i<l; i++) document.addEventListener ? a[i].addEventListener("click", hideShow, false) : a[i].attachEvent("onclick", hideShow);
           
       function hideShow(e){

	(e.preventDefault) ? e.preventDefault() : (e.returnValue = false);
	
	var targetID = (e.target || e.srcElement).getAttribute("data-toShow"),
		elementToShow = document.getElementById(targetID);

	elementToShow.style.height = (elementToShow.offsetHeight == 0 ? elementToShow.scrollHeight : "0") + 'px';

}

var h = document.querySelectorAll("[data-toShow]"),
	i = 0,
	l = a.length;

for (; i<l; i++) document.addEventListener ? a[i].addEventListener("click", hideShow, false) : a[i].attachEvent("onclick", hideShow);
            
          
    
    function hideShow(e){

	(e.preventDefault) ? e.preventDefault() : (e.returnValue = false);
	
	var targetID = (e.target || e.srcElement).getAttribute("data-toShow"),
		elementToShow = document.getElementById(targetID);

	elementToShow.style.height = (elementToShow.offsetHeight == 0 ? elementToShow.scrollHeight : "0") + 'px';

}

var i = document.querySelectorAll("[data-toShow]"),
	i = 0,
	l = a.length;

for (; i<l; i++) document.addEventListener ? a[i].addEventListener("click", hideShow, false) : a[i].attachEvent("onclick", hideShow);
           
       function hideShow(e){

	(e.preventDefault) ? e.preventDefault() : (e.returnValue = false);
	
	var targetID = (e.target || e.srcElement).getAttribute("data-toShow"),
		elementToShow = document.getElementById(targetID);

	elementToShow.style.height = (elementToShow.offsetHeight == 0 ? elementToShow.scrollHeight : "0") + 'px';

}

var j = document.querySelectorAll("[data-toShow]"),
	i = 0,
	l = a.length;

for (; i<l; i++) document.addEventListener ? a[i].addEventListener("click", hideShow, false) : a[i].attachEvent("onclick", hideShow);
            
          
   
    function hideShow(e){

	(e.preventDefault) ? e.preventDefault() : (e.returnValue = false);
	
	var targetID = (e.target || e.srcElement).getAttribute("data-toShow"),
		elementToShow = document.getElementById(targetID);

	elementToShow.style.height = (elementToShow.offsetHeight == 0 ? elementToShow.scrollHeight : "0") + 'px';

}

var k = document.querySelectorAll("[data-toShow]"),
	i = 0,
	l = a.length;

for (; i<l; i++) document.addEventListener ? a[i].addEventListener("click", hideShow, false) : a[i].attachEvent("onclick", hideShow);
           
       function hideShow(e){

	(e.preventDefault) ? e.preventDefault() : (e.returnValue = false);
	
	var targetID = (e.target || e.srcElement).getAttribute("data-toShow"),
		elementToShow = document.getElementById(targetID);

	elementToShow.style.height = (elementToShow.offsetHeight == 0 ? elementToShow.scrollHeight : "0") + 'px';

}

var l = document.querySelectorAll("[data-toShow]"),
	i = 0,
	l = a.length;

for (; i<l; i++) document.addEventListener ? a[i].addEventListener("click", hideShow, false) : a[i].attachEvent("onclick", hideShow);
            
          
   
    function hideShow(e){

	(e.preventDefault) ? e.preventDefault() : (e.returnValue = false);
	
	var targetID = (e.target || e.srcElement).getAttribute("data-toShow"),
		elementToShow = document.getElementById(targetID);

	elementToShow.style.height = (elementToShow.offsetHeight == 0 ? elementToShow.scrollHeight : "0") + 'px';

}

var m = document.querySelectorAll("[data-toShow]"),
	i = 0,
	l = a.length;

for (; i<l; i++) document.addEventListener ? a[i].addEventListener("click", hideShow, false) : a[i].attachEvent("onclick", hideShow);
           
       function hideShow(e){

	(e.preventDefault) ? e.preventDefault() : (e.returnValue = false);
	
	var targetID = (e.target || e.srcElement).getAttribute("data-toShow"),
		elementToShow = document.getElementById(targetID);

	elementToShow.style.height = (elementToShow.offsetHeight == 0 ? elementToShow.scrollHeight : "0") + 'px';

}

var n = document.querySelectorAll("[data-toShow]"),
	i = 0,
	l = a.length;

for (; i<l; i++) document.addEventListener ? a[i].addEventListener("click", hideShow, false) : a[i].attachEvent("onclick", hideShow);
            
          
    
    function hideShow(e){

	(e.preventDefault) ? e.preventDefault() : (e.returnValue = false);
	
	var targetID = (e.target || e.srcElement).getAttribute("data-toShow"),
		elementToShow = document.getElementById(targetID);

	elementToShow.style.height = (elementToShow.offsetHeight == 0 ? elementToShow.scrollHeight : "0") + 'px';

}

var o = document.querySelectorAll("[data-toShow]"),
	i = 0,
	l = a.length;

for (; i<l; i++) document.addEventListener ? a[i].addEventListener("click", hideShow, false) : a[i].attachEvent("onclick", hideShow);
           
       function hideShow(e){

	(e.preventDefault) ? e.preventDefault() : (e.returnValue = false);
	
	var targetID = (e.target || e.srcElement).getAttribute("data-toShow"),
		elementToShow = document.getElementById(targetID);

	elementToShow.style.height = (elementToShow.offsetHeight == 0 ? elementToShow.scrollHeight : "0") + 'px';

}

var p = document.querySelectorAll("[data-toShow]"),
	i = 0,
	l = a.length;

for (; i<l; i++) document.addEventListener ? a[i].addEventListener("click", hideShow, false) : a[i].attachEvent("onclick", hideShow);
            
          
    
    function hideShow(e){

	(e.preventDefault) ? e.preventDefault() : (e.returnValue = false);
	
	var targetID = (e.target || e.srcElement).getAttribute("data-toShow"),
		elementToShow = document.getElementById(targetID);

	elementToShow.style.height = (elementToShow.offsetHeight == 0 ? elementToShow.scrollHeight : "0") + 'px';

}

var q = document.querySelectorAll("[data-toShow]"),
	i = 0,
	l = a.length;

for (; i<l; i++) document.addEventListener ? a[i].addEventListener("click", hideShow, false) : a[i].attachEvent("onclick", hideShow);
           
       function hideShow(e){

	(e.preventDefault) ? e.preventDefault() : (e.returnValue = false);
	
	var targetID = (e.target || e.srcElement).getAttribute("data-toShow"),
		elementToShow = document.getElementById(targetID);

	elementToShow.style.height = (elementToShow.offsetHeight == 0 ? elementToShow.scrollHeight : "0") + 'px';

}

var r = document.querySelectorAll("[data-toShow]"),
	i = 0,
	l = a.length;

for (; i<l; i++) document.addEventListener ? a[i].addEventListener("click", hideShow, false) : a[i].attachEvent("onclick", hideShow);
            
          
   
    function hideShow(e){

	(e.preventDefault) ? e.preventDefault() : (e.returnValue = false);
	
	var targetID = (e.target || e.srcElement).getAttribute("data-toShow"),
		elementToShow = document.getElementById(targetID);

	elementToShow.style.height = (elementToShow.offsetHeight == 0 ? elementToShow.scrollHeight : "0") + 'px';

}

var s = document.querySelectorAll("[data-toShow]"),
	i = 0,
	l = a.length;

for (; i<l; i++) document.addEventListener ? a[i].addEventListener("click", hideShow, false) : a[i].attachEvent("onclick", hideShow);
           
       function hideShow(e){

	(e.preventDefault) ? e.preventDefault() : (e.returnValue = false);
	
	var targetID = (e.target || e.srcElement).getAttribute("data-toShow"),
		elementToShow = document.getElementById(targetID);

	elementToShow.style.height = (elementToShow.offsetHeight == 0 ? elementToShow.scrollHeight : "0") + 'px';

}

var t = document.querySelectorAll("[data-toShow]"),
	i = 0,
	l = a.length;

for (; i<l; i++) document.addEventListener ? a[i].addEventListener("click", hideShow, false) : a[i].attachEvent("onclick", hideShow);
            
          